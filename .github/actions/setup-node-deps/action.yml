name: Setup Vite+ dependencies
description: Install Vite+, Node.js, the configured package manager, and locked dependencies

runs:
  using: composite
  steps:
    - name: Setup Vite+
      uses: voidzero-dev/setup-vp@v1.17.0
      with:
        version: "0.2.9"
        node-version: "22"
        cache: true
        run-install: |
          - cwd: .
            args: ['--frozen-lockfile']
