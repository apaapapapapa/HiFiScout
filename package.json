{
  "name": "hifiscout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply hifiscout --local",
    "db:migrate:remote": "wrangler d1 migrations apply hifiscout --remote"
  },
  "devDependencies": {
    "wrangler": "^4.32.0"
  }
}
